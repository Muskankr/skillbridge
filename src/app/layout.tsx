import type { Metadata } from "next";
import { Geist, Geist_Mono } from "next/font/google";
import "./globals.css";

const geistSans = Geist({
  variable: "--font-geist-sans",
  subsets: ["latin"],
});

const geistMono = Geist_Mono({
  variable: "--font-geist-mono",
  subsets: ["latin"],
});

export const metadata: Metadata = {
  title: "SkillBridge – Build Your Developer Identity",
  description:
    "SkillBridge helps students build a professional developer profile, showcase projects, certificates, skills, and achievements — all from one place.",
  openGraph: {
    title: "SkillBridge – Build Your Developer Identity",
    description: "One Profile. Every Achievement.",
    url: "https://skillbridge-zxbg-kappa.vercel.app/",
    siteName: "SkillBridge",
    images: [
      {
        url: "/og-image.png",
        width: 1200,
        height: 630,
      },
    ],
    type: "website",
  },
  twitter: {
    card: "summary_large_image",
    title: "SkillBridge – Build Your Developer Identity",
    description: "One Profile. Every Achievement.",
    images: ["/og-image.png"],
  },
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html
      lang="en"
      className={`${geistSans.variable} ${geistMono.variable} h-full antialiased`}
    >
      <body className="min-h-full flex flex-col">{children}</body>
    </html>
  );
}